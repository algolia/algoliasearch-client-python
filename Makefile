# Minimal makefile for Sphinx documentation
# =========================================
# NOTE: This file was generated by `sphinx-quickstart`, then customized.
#
# Usage
# -----
# To generate the doc, just run:
#
# ```
# make clean html
# ```
#
# A clean build is required, otherwise some changes may not be taken into account.

# You can set these variables from the command line.
SPHINXOPTS    =
SPHINXBUILD   = sphinx-build
SPHINXPROJ    = AlgoliaAPIClientforPython
SOURCEDIR     = doc/reference
BUILDDIR      = doc/build

# Put it first so that "make" without argument is like "make help".
help:
	@$(SPHINXBUILD) -M help "$(SOURCEDIR)" "$(BUILDDIR)" $(SPHINXOPTS) $(O)

.PHONY: help Makefile

# Catch-all target: route all unknown targets to Sphinx using the new
# "make mode" option.  $(O) is meant as a shortcut for $(SPHINXOPTS).
%: Makefile
	@$(SPHINXBUILD) -M $@ "$(SOURCEDIR)" "$(BUILDDIR)" $(SPHINXOPTS) $(O)

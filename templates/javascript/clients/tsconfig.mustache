{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    {{#isAlgoliasearchClient}}
    "types": ["node", "jest"],
    {{/isAlgoliasearchClient}}
    "outDir": "dist"
  },
  "include": ["{{apiPackage}}", "model", "builds"],
  "exclude": ["dist", "node_modules"]
}

description: Success
content:
  application/json:
    schema:
      type: object
      title: SuccessResponse
      additionalProperties: false
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 200
          description: Status code.
        message:
          type: string
          example: Success
          description: Response message.
